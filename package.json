{
  "name": "qa-tests-playwright",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "report": "npx playwright show-report",
    "install:playwright": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.36.0",
    "@types/node": "^20.5.0",
    "playwright": "^1.36.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
