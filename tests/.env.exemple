# Proxy service (system under test)
PROXY_HOST=127.0.0.1
PROXY_PORT=8000
BASE_URL=http://127.0.0.1:8000

# Mock downstream server
MOCK_HOST=127.0.0.1
MOCK_PORT=8085

# Timeouts (ms)
TEST_TIMEOUT=30000
